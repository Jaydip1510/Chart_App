import React from 'react'

const ChatItem = ({
    avatar =[],
    name,
    _id,
    groupChat=false,
    sameSender,
    isOnline
}) => {
  return (
    <div>
      ChatItem
    </div>
  )
}

export default ChatItem
